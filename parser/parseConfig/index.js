const parseConfig = {
  parseCount: 0,
  browserConfig: {
    // devtools: true,
    // headless: false,
    args: ['--log-level="0"'],
    defaultViewport: null
  }
};

module.exports = parseConfig;
